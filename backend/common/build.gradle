bootJar {
    enabled = false
}

jar {
    enabled = true
    from(project(':entity').jar)
}

dependencies {
    implementation project(':entity')

    // mail
    implementation 'org.springframework.boot:spring-boot-starter-mail'
}

tasks.register("prepareKotlinBuildScriptModel"){}
